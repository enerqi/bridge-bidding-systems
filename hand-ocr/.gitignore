# uv-managed virtual environment (recreate with `uv sync`)
.venv/

# Python bytecode / caches
__pycache__/
*.py[cod]

# tool caches
.ruff_cache/
.pytest_cache/
.ty_cache/

# Claude Code local state (per-checkout; not shared)
.claude/
